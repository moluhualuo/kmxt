<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:icon="@mipmap/kmxt_launcher"
        android:label="@string/kmxt_app_name"
        android:roundIcon="@mipmap/kmxt_launcher_round">
        <activity
            android:name="top.moluhualuo.kmxt.KmxtTestLauncherActivity"
            android:exported="true"
            android:icon="@mipmap/kmxt_launcher"
            android:label="@string/kmxt_app_name"
            android:roundIcon="@mipmap/kmxt_launcher_round">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
